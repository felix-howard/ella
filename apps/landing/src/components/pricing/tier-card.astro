---
/**
 * Pricing card — marketing card for worker-count service ranges.
 *
 * Displays:
 *  - Headline price = typical bundle (marketingMonthly from pricing-constants).
 *  - Optional `breakdown` list revealing how the bundle adds up, so users
 *    can mentally reconcile it against the interactive calculator below.
 *    Smaller ranges show strict sums (service base + payroll base).
 *    Larger ranges use softer "includes" copy since $425 isn't a clean sum.
 *  - Optional `priceNote` subtitle under the price (e.g. "typical salon setup").
 *
 * The middle worker range gets `popular=true` for the "Most Popular" badge.
 */
import { checkPath } from "@/lib/icons";
import { siteConfig } from "@/config/site";

export interface BreakdownRow {
  /** Item label, e.g. "Monthly service (≤10 workers)". */
  label: string;
  /** Optional dollar amount; when omitted we render label only. */
  amount?: number;
  /** Rate unit, defaults to `/mo`. */
  unit?: string;
}

interface Props {
  id: "basic" | "pro" | "vip";
  name: string;
  tagline: string;
  /** Display-only marketing price (USD/month). */
  monthlyPrice: number;
  /** Short caption under the headline price, e.g. "typical salon setup". */
  priceNote?: string;
  /** Fixed bookkeeping setup fee in USD; omit if custom (use setupNote instead). */
  setupFee?: number;
  /** Fixed payroll setup fee in USD; rendered as a second setup line when present. */
  payrollSetupFee?: number;
  /** Free-form setup copy, used when no fixed fee. */
  setupNote?: string;
  /** Optional formula rows shown under price — explains how `monthlyPrice` adds up. */
  breakdown?: readonly BreakdownRow[];
  /** Heading above breakdown rows ("How it adds up" / "What's included"). */
  breakdownHeading?: string;
  bullets: readonly string[];
  popular?: boolean;
  ctaLabel?: string;
  ctaHref?: string;
}

const {
  id,
  name,
  tagline,
  monthlyPrice,
  priceNote,
  setupFee,
  payrollSetupFee,
  setupNote,
  breakdown,
  breakdownHeading = "How it adds up",
  bullets,
  popular = false,
  ctaLabel = siteConfig.cta.navLabel,
  ctaHref = siteConfig.cta.primaryHref,
} = Astro.props;

// When a fixed setup fee is provided, render bookkeeping + payroll setup lines.
const setupLines: string[] = [];
if (setupFee !== undefined) {
  setupLines.push(`+ $${setupFee} bookkeeping one-time setup`);
  if (payrollSetupFee !== undefined) {
    setupLines.push(`+ $${payrollSetupFee} payroll one-time setup`);
  }
} else if (setupNote) {
  setupLines.push(setupNote);
}
---

<article
  id={`tier-${id}`}
  class:list={[
    "service-card service-card-interactive relative flex h-full flex-col p-6 sm:p-8",
    popular
      ? "border-primary-400 shadow-xl shadow-primary-900/10 lg:scale-105"
      : "",
  ]}
  data-animate="fade-up"
  aria-labelledby={`tier-${id}-name`}
>
  {popular && (
    <span class="absolute -top-4 left-1/2 -translate-x-1/2 rounded-full bg-primary-600 px-4 py-1 text-xs font-semibold uppercase tracking-wide text-white">
      Most Popular
    </span>
  )}

  <div class="mb-6">
    <h3 id={`tier-${id}-name`} class="text-lg font-bold text-slate-950 font-heading">
      {name}
    </h3>
    <p class="mt-1 text-sm leading-6 text-slate-600">{tagline}</p>
  </div>

  <div class="mb-5">
    <div class="flex items-baseline gap-1">
      <span class="text-4xl font-extrabold tabular-nums text-slate-950">
        ${monthlyPrice}
      </span>
      <span class="text-base text-slate-500">/mo</span>
    </div>
    {priceNote && (
      <p class="mt-1 text-xs font-medium uppercase tracking-wide text-gray-400">
        {priceNote}
      </p>
    )}
    {setupLines.map((line) => (
      <p class="mt-1 text-xs text-gray-500">{line}</p>
    ))}
  </div>

  {breakdown && breakdown.length > 0 && (
    <div
      class:list={[
        "mb-6 rounded-xl px-4 py-3 text-sm",
        popular
          ? "bg-primary-50/70 ring-1 ring-primary-100"
          : "bg-gray-50 ring-1 ring-gray-100",
      ]}
      aria-label={breakdownHeading}
    >
      <p class="mb-2 text-[11px] font-semibold uppercase tracking-wider text-gray-500">
        {breakdownHeading}
      </p>
      <ul class="space-y-1.5" role="list">
        {breakdown.map((row) => (
          <li class="grid gap-1 lg:grid-cols-[minmax(0,1fr)_auto] lg:items-baseline lg:gap-3">
            <span class="min-w-0 text-gray-600">{row.label}</span>
            {row.amount !== undefined && (
              <span class="whitespace-nowrap font-medium tabular-nums text-gray-900">
                ${row.amount}
                <span class="ml-0.5 text-xs font-normal text-gray-400">
                  {row.unit ?? "/mo"}
                </span>
              </span>
            )}
          </li>
        ))}
      </ul>
    </div>
  )}

  <a
    href={ctaHref}
    class:list={[
      "mb-8 inline-flex min-h-11 w-full cursor-pointer items-center justify-center rounded-full px-5 py-3 text-center text-sm font-semibold transition-colors focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-500",
      popular
        ? "bg-accent-700 text-white hover:bg-accent-800"
        : "border border-primary-600 text-primary-600 hover:bg-primary-50",
    ]}
  >
    {ctaLabel}
  </a>

  <ul class="flex-1 space-y-3" role="list">
    {bullets.map((bullet) => (
      <li class="flex items-start gap-3">
        <svg
          class="mt-0.5 h-5 w-5 flex-shrink-0 text-primary-600"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
          stroke-width="2"
          aria-hidden="true"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d={checkPath}
          />
        </svg>
        <span class="text-sm text-gray-600">{bullet}</span>
      </li>
    ))}
  </ul>
</article>
