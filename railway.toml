[build]
watchPaths = ["apps/api/**", "packages/**"]

[deploy]
startCommand = "pnpm --filter @ella/api start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
