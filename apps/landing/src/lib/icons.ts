/**
 * Shared SVG path constants (Heroicons outline) reused across landing pages.
 * Single source of truth — avoids duplicating paths in multiple .astro files.
 */

/** Checkmark icon path (Heroicons outline) */
export const checkPath = "M4.5 12.75l6 6 9-13.5";

/** X-mark / close icon path (Heroicons outline) */
export const xPath = "M6 18L18 6M6 6l12 12";
